// Legacy export — see ChairButton.tsx. Kept as a re-export so any straggler
// imports don't break during the rebrand, but new code should import ChairButton.
export { default } from "./ChairButton";
